const Header = () => {
    return (
        <div className="header">
            <textarea
                name="text-area"
                id="text-area"
                className="text-area"
                placeholder="Type your query here..."
            ></textarea>
        </div>
    )
}

export default Header;